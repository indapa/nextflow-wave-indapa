{
    "name": "nextflow-dev",
    "build": {
        "dockerfile": "Dockerfile",
        "context": "."
    },
    "settings": {
        // Optional: Set VS Code settings, for example Python formatting or linting
        "python.defaultInterpreterPath": "/usr/local/bin/python"
    },
    "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
    ],
    "remoteUser": "vscode"
}
